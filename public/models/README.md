# 🚁 Drone Model Installation Guide

## Куда копировать файлы

### 1. FBX модель дрона
```
public/models/drone.fbx
```

### 2. Текстуры (если есть отдельные файлы)
```
public/models/textures/
├── texture_diffuse.png
├── texture_normal.png
├── texture_metallic.png
└── ... (другие текстурные файлы)
```

---

## Как это работает

### Автоматическая загрузка
При старте приложение автоматически пытается загрузить модель из `/models/drone.fbx`.

Если файл найден:
- ✓ FBX модель загружается с текстурами
- ✓ Автоматически масштабируется до ~2.5 метров
- ✓ Центрируется относительно геометрии
- ✓ Включаются тени
- ✓ Текстуры настраиваются для корректного отображения

Если файл НЕ найден:
- ⚠ Используется placeholder модель (квадрокоптер из примитивов)
- ⚠ В консоли появится предупреждение

### Поддерживаемые форматы текстур
FBXLoader автоматически загружает текстуры, встроенные в FBX файл, или ищет их:
- В той же папке что и FBX файл
- В подпапке `textures/`

Поддерживаемые форматы:
- PNG, JPG, JPEG
- TGA, BMP
- DDS (с ограничениями)

---

## Требования к FBX модели

### Рекомендуемые параметры:
- **Масштаб**: Любой (автоматически нормализуется)
- **Ориентация**: Нос дрона должен смотреть вдоль **-Z оси** в Blender/Maya
- **Размер**: Оптимально 1-10 метров (автоматически масштабируется до 2.5м)
- **Полигоны**: До 50,000 треугольников для плавной работы
- **Текстуры**: До 2048x2048px каждая

### Проверка ориентации модели:
В вашем 3D редакторе:
1. Нос дрона должен смотреть в **-Z** направление (назад)
2. Верх дрона должен смотреть в **+Y** направление (вверх)
3. Правое крыло в **+X** направление (вправо)

---

## Ручная загрузка модели

Если нужно загрузить модель программно:

```javascript
// В main.js или где создаётся DroneModel
const droneModel = new DroneModel(sceneManager);

// Загрузить пользовательскую модель
await droneModel.loadCustomModel('/models/my-custom-drone.fbx', 3.0);
// где 3.0 - желаемый размер в метрах
```

---

## Устранение проблем

### Модель слишком большая/маленькая
Измените второй параметр при загрузке:
```javascript
await droneModel.loadCustomModel('/models/drone.fbx', 5.0); // 5 метров
```

### Модель неправильно ориентирована
Если нос дрона смотрит не туда:
1. Откройте модель в Blender
2. Поверните так, чтобы нос смотрел в -Z направление
3. Apply Rotation (Ctrl+A → Rotation)
4. Экспортируйте заново

### Текстуры не загружаются
1. Убедитесь что текстуры в той же папке или в `textures/`
2. Проверьте имена файлов (должны совпадать со ссылками в FBX)
3. Используйте относительные пути при экспорте FBX

### Модель не видна
Проверьте консоль браузера (F12):
- Есть ли сообщение "✓ FBX model loaded successfully"?
- Есть ли ошибки загрузки?

---

## Экспорт FBX из популярных редакторов

### Blender
1. File → Export → FBX (.fbx)
2. Настройки:
   - ☑ Selected Objects (если нужно только дрон)
   - ☑ Apply Modifiers
   - ☑ Bake Animation: OFF
   - Path Mode: **Copy**
   - ☑ Embed Textures
   - Forward: **-Z Forward**
   - Up: **Y Up**

### Maya
1. File → Export All → FBX Export
2. Настройки:
   - Axis Conversion: Z-up → Y-up
   - Embed Media: ON

### 3ds Max
1. Export → FBX
2. Axis Conversion: Y-up
3. Embed Media: Checked

---

## Примеры использования

### Базовое использование (автоматическое)
Просто положите файл `drone.fbx` в эту папку - всё остальное произойдёт автоматически!

### Программная загрузка
```javascript
// main.js
import { DroneModel } from './src/scene/DroneModel.js';

const droneModel = new DroneModel(sceneManager);

// Дождаться загрузки (опционально)
setTimeout(async () => {
    if (droneModel.isPlaceholder) {
        console.log('Trying manual load...');
        await droneModel.loadCustomModel('/models/drone.fbx');
    }
}, 1000);
```

---

## Технические детали

### Что делает загрузчик:
1. **Загружает FBX** через FBXLoader
2. **Вычисляет размеры** через Bounding Box
3. **Автоматически масштабирует** до нужного размера
4. **Центрирует модель** относительно геометрии
5. **Настраивает текстуры** (sRGB color space)
6. **Включает тени** для всех mesh объектов
7. **Сохраняет позицию/ориентацию** при замене модели

### Производительность:
- Загрузка происходит асинхронно (не блокирует UI)
- Показывается прогресс в консоли
- При ошибке автоматически возвращается к placeholder
- Правильная очистка памяти при dispose()

---

## Контрольный чеклист

- [ ] FBX файл скопирован в `public/models/drone.fbx`
- [ ] Текстуры (если есть) в `public/models/textures/`
- [ ] Открыл приложение в браузере
- [ ] Проверил консоль (F12) на наличие "✓ FBX model loaded successfully"
- [ ] Модель отображается с правильным размером
- [ ] Модель правильно ориентирована (нос смотрит вперёд)
- [ ] Текстуры применились корректно
- [ ] Модель вращается согласно attitude данным

---

Если нужна помощь - проверьте консоль браузера для диагностики!
